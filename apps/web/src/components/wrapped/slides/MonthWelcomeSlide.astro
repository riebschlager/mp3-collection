---
/**
 * MonthWelcomeSlide - Opening slide for monthly wrapped story
 */
import StatCard from '../StatCard.astro';

export interface Props {
  monthLabel: string;
  totalScrobbles: number;
}

const { monthLabel, totalScrobbles } = Astro.props;
---

<div class="slide-content text-center">
  <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold mb-8">
    Your {monthLabel} <br />in Music
  </h1>
  <StatCard
    value={totalScrobbles.toLocaleString()}
    label="total scrobbles"
    sublabel="Let's explore your month"
    size="md"
  />
  <div class="mt-12 text-lg opacity-75 animate-bounce">
    Scroll to explore ↓
  </div>
</div>
